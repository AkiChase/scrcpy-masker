// use bevy::{prelude::*, window::PresentMode};



// pub struct BasicPlugin;

// impl Plugin for BasicPlugin {
//     fn build(&self, app: &mut App) {
//         app.add_systems(Startup, (setup, border.after(setup), show.after(border)));
//     }
// }

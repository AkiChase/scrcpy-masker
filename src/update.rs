pub fn check_for_update() {
    // TODO check for update, if confirmed, copy current to xxx.{version}.old, run xxx.{version}.old --upgrade, exit current app
    // TODO github or toolsetlink?
}

pub fn self_update() {
    // TODO download new version, copy to xxx+.latest, run xxx+.latest, exit current app
}
